# Upstream patches

No upstream patches are used in v1.

If a patch becomes unavoidable, add a numbered patch under a directory named for the upstream project and document the reason, exact applicable commit, upstream issue/PR, and objective removal condition here. Dockerfiles must apply it explicitly; never edit imported source silently.
