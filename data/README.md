# Data

Place small local symlinks/directories here or set `DATA_DIR` in `.env` to an external dataset root. The directory is mounted read-only at `/data`. Bags and datasets are ignored by Git.

Always run `make validate-bag ...` before a long mapping run.
